//გაქვს მასივი, სადაც წერია როგორც დადებითი, ისე უარყოფითი რიცხვები. დაითვალე, რამდენი უარყოფითი რიცხვია მასივში.
let arr = [1, -2, 3, -4, 5, 10, -1, -5];
let count = 0;
for (let i = 0; i < arr.length; i++) {
    if (arr[i] < 0) {
        count++;
    }
}
    console.log(count);
