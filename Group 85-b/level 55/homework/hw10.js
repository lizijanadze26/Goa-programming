//დაწერეთ კოდი, რომელიც აიღებს მასივს [1,2,3,4,5], და გამოიტანოს მხოლოდ ლუწი რიცხვები

let arr = [0,1,2,3,4,5];
for (let i = 0; i < arr.length; i++) {
    if (arr[i] % 2 === 0) {
        console.log(arr[i]);
    }
}