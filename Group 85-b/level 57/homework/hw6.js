//6. do... while loop-ის გამოყენებით დაბეჭდეთ ელემენტები უკუღმა, გამოიყენეთ მასივი: 
let array = [1, 2, 3, 4, 5];
let index = array.length - 1;
do {
    console.log(array[index]);
    index--;
} while (index >= 0);